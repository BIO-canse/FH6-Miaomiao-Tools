# AutoInputLoop

双击运行后，程序会循环执行：

1. 等待 10 秒
2. 鼠标左键按下 0.05 秒后抬起
3. 等待 10 秒
4. `W` 按下 0.1 秒后抬起
5. 等待 10 秒
6. `D` 按下 0.1 秒后抬起

另外还有一个独立循环：启动等待 10 秒后，`X` 每隔 5 秒按下 0.1 秒后抬起。

按 `Space+C` 退出。第一次等待的 10 秒内，请切换到你要接收鼠标和键盘输入的窗口。

如果目标程序是以管理员权限运行的，这个程序也需要用管理员权限运行。
