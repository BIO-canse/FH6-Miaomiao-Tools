# legacy-scripts

这里归档之前做过的简单脚本。

全自动流程已经覆盖了 `MinuteWLoop` 和 `SpaceDownEnterLoop` 的主要用户用途，所以这两个脚本只作为全自动流程内部组件和源码归档保留，不再提供单独用户说明。

- `src/`：C# 源码
- `bin/`：可直接双击运行的 exe
- `build/`：构建脚本
- `docs/`：仍然需要单独运行的脚本说明

重新编译时，运行 `build/` 下对应脚本即可，输出会覆盖 `bin/` 下同名 exe。
